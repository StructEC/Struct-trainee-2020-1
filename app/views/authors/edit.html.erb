<h1>Editar Autor</h1>

<%= form_with model: @author do |form| %>
  <% if @author.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@author.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @author.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, 'Nome' %>
    <%= form.text_field :name, value: @author.name  %>
  </div>

  <div class="field">
    <%= form.label :email, 'E-mail' %>
    <%= form.email_field :email, value: @author.email %>
  </div>

  <div class="field">
    <%= form.label :year, 'Ano' %>
    <%= form.number_field :year, value: @author.year %>
  </div>

  <div class="actions">
    <%= form.submit "Atualizar autor" %>
  </div>
<% end %>

<%= link_to 'Voltar', authors_path %>

